Custom minimap icon

Put your square custom minimap texture here as:

MinimapIcon.tga
or
MinimapIcon.blp

The addon looks for:
Interface/AddOns/HealingPriorityMouse/Media/MinimapIcon

If that texture is missing, it falls back to the built-in gold cross icon.
